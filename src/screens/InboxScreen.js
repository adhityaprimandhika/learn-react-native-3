import { Text, View } from "react-native";

const InboxScreen = ({ navigation }) => {
  return (
    <View
      style={{
        paddingTop: 60,
        paddingHorizontal: 16,
        alignItems: "center",
      }}
    >
      <Text>Ini adalah InboxScreen</Text>
    </View>
  );
};

export default InboxScreen;
