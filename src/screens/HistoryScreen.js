import { Text, View } from "react-native";

const HistoryScreen = ({ navigation }) => {
  return (
    <View
      style={{
        paddingTop: 60,
        paddingHorizontal: 16,
        alignItems: "center",
      }}
    >
      <Text>Ini adalah HistoryScreen</Text>
    </View>
  );
};

export default HistoryScreen;
